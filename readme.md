### Sort numbers challenge

Since .sort() method sorts array values alphabetically, for 
learning purposes challenged myself to create my own method
for numbers sorting.

Instead of using: 
.sort((a, b) => {
  return a - b
})